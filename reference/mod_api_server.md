# API Data tab module server

API Data tab module server

## Usage

``` r
mod_api_server(id, raw_usage_rv)
```

## Arguments

- id:

  Shiny module ID.

- raw_usage_rv:

  \`reactiveVal\` holding the full usage tibble. API data can be merged
  into this value when the user confirms.
