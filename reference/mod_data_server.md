# Data tab module server

Data tab module server

## Usage

``` r
mod_data_server(id, raw_usage_rv)
```

## Arguments

- id:

  Shiny module ID.

- raw_usage_rv:

  \`reactiveVal\` holding the full usage tibble.
