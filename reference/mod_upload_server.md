# Upload Data tab module server

Upload Data tab module server

## Usage

``` r
mod_upload_server(id, raw_usage_rv)
```

## Arguments

- id:

  Shiny module ID.

- raw_usage_rv:

  \`reactiveVal\` holding the full usage tibble. Uploaded data is merged
  into this value.
