% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_data.R
\name{util_summarise_sessions}
\alias{util_summarise_sessions}
\title{Summarise token usage by session}
\usage{
util_summarise_sessions(usage)
}
\arguments{
\item{usage}{Tibble from [util_read_claude_code()] or [util_combine_usage()].
Cost is computed automatically if `cost_usd` is absent.}
}
\value{
One-row-per-session tibble with `session_id`, `project`, `machine`,
  `source`, `date`, `duration_min`, `models`, `n_requests`, token columns,
  `total_tokens`, `cost_usd`, `outlier`. Ordered by descending `cost_usd`.
}
\description{
Aggregates to one row per session (`session_id`). Outliers are sessions
whose cost exceeds mean + 2 SD. When SD is zero or undefined no session
is flagged.
}
